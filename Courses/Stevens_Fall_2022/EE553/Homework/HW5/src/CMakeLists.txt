project(f_func)
# no need to link this is just for the .dat file

#set(HEADER_FILES classComplex.h functionArray.h)
#set(SOURCE_FILES classComplex.cpp functionArray.cpp)

#add_library(f_func ${SOURCE_FILES})